from django.urls import path
from django.contrib import admin
from SLFAPP import views
urlpatterns = [
    # path('admin/', admin.site.urls),
    # path('usignup/', views.name),
    # path('page/', views.rendpage),

]
